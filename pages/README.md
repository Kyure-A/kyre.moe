This directory is intentionally empty.

Next.js prioritizes the root "pages" directory over "src/pages".
We keep this folder to prevent Next from treating "src/pages" (Feature-Sliced Design layer) as routing pages.
